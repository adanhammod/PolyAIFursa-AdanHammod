apiVersion: v1
kind: Service
metadata:
  name: agent
  namespace: prod
  labels:
    app: agent
spec:
  type: ClusterIP
  selector:
    app: agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
