apiVersion: v1
kind: Service
metadata:
  name: agent-nodeport
  namespace: dev
  labels:
    app: agent
spec:
  type: NodePort
  selector:
    app: agent
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30800
      protocol: TCP
